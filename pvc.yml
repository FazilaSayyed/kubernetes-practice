kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: local-pvc
  namspace: nginx
spec:
  accessModes:
   - ReadWriteOnce
  resources:
    request: 
      storage: 1Gi
  storageClassName: local-storage
